# Core data + Excel
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
msoffcrypto-tool>=5.4

# .msg-mail parsing (extract.py / dry_run.py)
extract-msg>=0.48

# SAF-T-parsning (load_saft.py / saft_parser.py): xsdata-genererade dataclasses
# ur Norwegian_SAF-T_Financial_Schema (saft_schema_no/), lxml för streamande
# per-Transaction-parsning av stora filer.
xsdata[cli]>=24.0
lxml>=5.0

# Lokal dev / migrationskälla. Kvarstår även efter Postgres-cutover eftersom
# scripts/migrate_duckdb_to_postgres.py läser data/finance.duckdb.
duckdb>=1.0

# Postgres-driver (Azure DB for PostgreSQL Flexible Server)
psycopg[binary]>=3.2
psycopg-pool>=3.2

# Markdown-tabeller i mcp_server.py (pandas df.to_markdown)
tabulate>=0.9

# Webapp
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.20
sse-starlette>=2.0

# Easy Auth-headerdekod (X-MS-CLIENT-PRINCIPAL)
pyjwt>=2.8

# HTTP-klienter (mcp_server, leverantörer)
requests>=2.32
httpx>=0.27

# MCP-server (utvecklarverktyg)
mcp>=1.0

# Azure-integrationer (App Service Managed Identity, Key Vault, Blob)
azure-identity>=1.17
azure-keyvault-secrets>=4.8
azure-storage-blob>=12.20

# Lokal kontrollpanel (gui.py) — körs inte i App Service-containern
PySide6>=6.7
